// Deployed DEV environment (Cloudflare Pages, development branch): call the dev
// backend on Render via its custom domain. Not a secret — a public API URL.
export const environment = {
  production: true,
  apiBaseUrl: 'https://dev.api.rieakane.com',
};
